{
  "name": "@australis/express-plain-text-error-handler",
  "version": "0.1.0-1",
  "description": "Express plain text error handler, Sends error message to client's request's statusText",
  "private": false,
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "jest --env=node --ci --config=./jest.json",
    "build": "rimraf lib && tsc -p .",
    "publishit": "npm publish --access public"
  },
  "keywords": [],
  "author": "d10221",
  "license": "MIT",
  "dependencies": {
    "@australis/create-debug": "^1.0.2",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/jest": "^23.3.9",
    "jest": "23.6.0",
    "rimraf": "^2.6.2",
    "ts-jest": "23.10.4",
    "ts-node": "^7.0.1",
    "typescript": "3.1.6"
  },
  "files": [
    "lib/**/*"
  ]
}
